import styled from 'styled-components';

export const ToggleButton = styled.button`
	display: block;
	width: fit-content;
	text-align: center;
	border: none;
	border-radius: 100px;
	cursor: pointer;
	padding: 0.4rem;
	background: transparent;
	color: white;
`;
