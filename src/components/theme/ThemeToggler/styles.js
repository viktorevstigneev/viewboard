import styled from 'styled-components';

export const ToggleButton = styled.button`
	border: none;
	border-radius: 100px;
	cursor: pointer;
	padding: 0.4rem;
	background: transparent;
	color: white;
`;
