import { takeEvery } from 'redux-saga/effects';

// import { GET_CURRENT_USER, GET_LANGUAGES } from '../constants';
// import { getCurrentUser, getLanguages } from './worker';

export default function* sagaWatcher() {
	// yield takeEvery(GET_CURRENT_USER, getCurrentUser);
	// yield takeEvery(GET_LANGUAGES, getLanguages);
}
